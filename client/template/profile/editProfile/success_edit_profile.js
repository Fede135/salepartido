//Con esto intento capturar el evento cuando se presiona el boton guardar asi redirijo a la pantalla de que se guardo con exito
/*
Template.successEditProfile.events({ 
  'click #successEditProfile': function(event) { 
     Router.go('successEditProfile', {_id : Meteor.userId()});
  } 
});*/ 
