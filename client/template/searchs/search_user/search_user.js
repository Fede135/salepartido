//Todo explotado  

/*Tracker.autorun(function () {
  let cursor = UsersIndex.search('Fede'); // search all docs that contain "Marie" in the name or score field

  console.log(cursor.fetch()); // log found documents with default search limit
  console.log(cursor.count()); // log count of all found documents
});

Template.searchBox.helpers({
  inputAttributes: function () {
    return { 'class': 'easy-search-input', 'placeholder': 'Ingrese nombre de amigo' };
  },
  usersIndex: () => UsersIndex
})*/